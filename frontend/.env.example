# The DataQuest read API the dashboard fetches from (the shipped FastAPI service).
# Local dev: the backend runs on :8000. Override per environment; never commit a real .env.
VITE_BACKEND_URL=http://localhost:8000